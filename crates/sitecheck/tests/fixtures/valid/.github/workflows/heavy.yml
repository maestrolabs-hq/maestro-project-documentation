# Minimal heavy workflow file required by the valid repository fixture.
name: Fixture Heavy

on: workflow_dispatch

permissions: {}

jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
